version: '3'

services:

  todo-app:
    image: vikyath11/todo-app-artifactory
    ports:
      - "3001:3001"

  mongodbcont:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db

volumes:
  mongodb-data:
